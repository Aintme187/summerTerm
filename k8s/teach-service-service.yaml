apiVersion: v1  # Kubernetes API 版本
kind: Service  # 资源类型，表示这是一个 Service 对象
metadata:
  name: teach-service  # Service 的名字
spec:
  ports:
  - port: 6888  # Service 公开的端口
    targetPort: 6888  # 转发到容器的端口
  selector:
    app: teach-service  # Service 选择的 Pod 的标签
